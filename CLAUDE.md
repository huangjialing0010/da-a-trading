# 大A 虚拟盘交易系统

## 项目定位
A股虚拟盘交易系统。不连接真实账户，所有交易在本地模拟。
**双策略并行**：深价仓做左侧均值回归，趋势 V2 做右侧盈利改善验证；两仓账户、资金和表现完全隔离。
趋势 V2 于 2026-08-07 从 200 万空仓启动，最早 2027-02-07 审查。深价仓受 ERP 分位仓位上限与行业集中度约束。
系统缩小候选范围，筛选器只是初筛。普通深价首次买入必须先有书面研究和最小结构化 `BUY` 建议，日更通过风险闸门后自动生成 T+1 虚拟订单；趋势 V2 按冻结的原策略自动执行，仅供纸上验证，允许尚未完成增强研究。

## 双仓架构
| | 深价仓 | 趋势虚拟仓（纸上验证） |
|------|----------|------------|
| 账户文件 | `account.json` | `account_trend_v2.json`（旧 `account_trend.json` 只读） |
| 资金 | 100万初始资金；当前状态以最新日报为准 | 200万初始资金（纸上） |
| 策略 | 跌40%+营收正增长+财报好，结构化建议后自动生成虚拟首仓订单 | 利润趋势改善+质量过滤，自动执行 |
| 候选池 | `candidates.csv` | `trend_candidates.csv` |
| 交易记录 | `trades.csv` | `trades_trend_v2.csv` |
| 表现追踪 | `performance.csv` | `performance_trend_v2.csv` |
| 复盘 | `weekly_*.md` / `monthly_*.md` | `trend_weekly_*.md` / `trend_monthly_*.md` |

深价仓新投入受 ERP 动态上限与行业集中度约束；超限不强制卖出现有持仓。趋势仓仅做纸上验证，非实盘。

## 文档路由
- 使用入口与安全规则：`README.md`
- 设计状态索引：`docs/README.md`
- 已落地变更与未完成事项：`docs/CHANGES.md`
- 详细设计依据：`docs/superpowers/specs/`

## 目录结构
- `config.yaml`：策略参数与回测摘要。
- `data/`：行情、财务和市场缓存；只有 `data/market/` 入 Git，日K线与财务缓存不入 Git。
- `output/account*.json`、`trades*.csv`、`performance*.csv`：双仓账户、成交和表现。
- `output/paper_orders*.json`：T+1 订单审计账本；历史订单不得删除。
- `output/candidates*.csv`、`trend_candidates.csv`、`candidate_tracker.csv`：候选与前瞻追踪。
- `output/research/`：书面研究；`output/reports/`：日报、周报、月报和索引。
- `tools/`：账户、订单、数据、筛选、信号、交易、验证和报告模块。
- `tests/`：标准库 `unittest`；`.github/workflows/daily.yml`：工作日自动日更。
- `main.py`：CLI；`requirements.txt`：Python 3.12 依赖锁。

## 回测结论（2026-07-24更新，全300池+营收过滤）
- **深价策略（全300，营收<0淘汰）**：+46.4% vs 基准+60.1%，超额-13.7pp，256笔交易，胜率34.2%，盈亏比2.58，最大回撤-35.3%
  - 营收过滤是决定性变量：不加营收过滤策略-21.3%，加入后+46.4%，单条过滤贡献67pp超额改善
  - 熊市年（21/22/23）系统性跑赢基准，牛市年（19/20/24）跑输——深度价值固有特征
  - 2019零交易（牛市无标的可买，策略基因）、2020亏-21%为COVID黑天鹅（报告延迟，硬止损有效防御）
- **趋势反转策略（利润趋势改善+质量过滤+MA200退出，无负债率约束）**：+79.0% vs 基准+65.9%，超额+13%，209笔交易，胜率44%，盈亏比2:0，最大回撤-46%
- **生存偏差警告**：两个策略均用当前沪深300成分股回测，未被剔除的股票已不存在。A股指数成分股年化生存偏差估计 1-3%，趋势策略真实超额可能在 0~10% 区间，深价策略真实超额可能在 -20~-5pp 区间

## 深价策略参数
- **新买入规则（已生效 2026-08-04）**：ERP 分位动态仓位上限替代旧版一刀切冻结。市场便宜时仓位放开、贵时收紧，不绝对禁止
  - ERP>80%分位→仓位上限 80% | 50-80%分位→50% | 20-50%分位→30% | <20%分位→20%
  - 超限处置：仓位上限和行业集中度只约束新增资金投入（含加仓），不构成卖出理由；超限时禁止买入/加仓，已有持仓按止损/止盈/到期自然退出
  - 执行位置：自动分批加仓、恐慌策略买入、`main.py trade` 手动买入、信号生成均校验；ERP 数据不可用时按 30% 保守上限执行
- **2027-01-01审查**：跑输沪深300超10个百分点→策略降级，只管理现有持仓不新增
- 硬止损：**-20%**，MA200跌破+亏损>10%（替代旧版180天时间止损）
- 跌幅阈值：≥40%
- 移动止盈：**+25%启动 / -12%回撤**
- 最短持有6月/最长18月
- 股票池：沪深300全300只（2026-07-24从70大扩至全300）
- 财务硬过滤：ROE<6%淘汰（年报）、经营现金流为负淘汰（年报）、利润同比-20%淘汰（最新报告期）、**营收同比<0淘汰**（最新报告期，回测验证单条过滤+67pp超额改善）
- 行业过滤：手工黑名单+加分名单已全部移除（主观判断不应左右系统）。仅保留商品周期检测 + 行业集中度（单申万二级≤2只）

## 趋势反转策略参数（纸上测试）
- 入场：利润YoY连续2个同类型报告期改善（≥2pp），ROE>6%，不追52周高点（<90%分位），商品周期高位不买（工业/能源>60%分位跳过）
- 退出：同深价（硬止损/MA200/移动止盈/到期）；硬止损或MA200卖出后触发20交易日冷却期，冷却期内不重新入场
- 仓位：最多5只，单只20%资金
- 质量过滤：跳过改善>500pp（基数效应）、改善<5pp（噪音）、利润YoY<-20%（仍在恶化）；负债率不过滤（回测证实高负债重资产公司是趋势改善主要来源，过滤后超额转负）

### 趋势仓纸上测试规则
- **中间不干预**：纸上测试期间不做任何参数调整。中途观察到的超额波动/连败/回撤均为正常噪声——回测 209 笔才有统计意义，小样本判断策略失效是伪命题。中途调参 = 过拟合搬到纸上 = 测试作废
- **数据熔断**：按交易日历和北京时间16:30推导期望完整交易日；趋势仓逐只核对全部持仓K线，任一缺失或落后时冻结趋势仓买卖和账户/表现写入。沪深300基准落后只冻结基准/超额表现写入，不阻断持仓数据正常时的交易
- **候选母集**：只接受经校验的沪深300成分缓存（250–350只且来源标记一致）；接口失败仅使用最后有效缓存，无有效缓存则冻结候选刷新。趋势候选先应用既有 5–500pp、ROE≥6%、当前利润同比≥-20% 过滤，再取TOP10，不改变策略阈值
- **持仓日期**：每只趋势持仓从交易记录重建最近一次由空仓变为持仓的日期；分批买入和部分卖出不重置。无法可靠重建时，硬止损和移动止盈继续有效，跳过MA200持有期退出和最长持有期退出并在日报告警
- **结构性检查**：连续 6 个月零笔交易（不是亏，是不触发）→ 策略在真实市场找不到标的 → 结构性失败 → 提前终止
- **V2终审**（V2启动满6个月且不早于2027-01-01，需满足全部条件）：
  - 完整买卖回合 ≥ 30（单只股票持仓从0变为正数、再回到0算1个完整回合；分批买入和部分卖出不单独计数）
  - 累计超额 > 0%（vs 沪深300）
  - 最大回撤 > -50%（即回撤不超过50%；与回测 -46% 一致即通过）
- **样本不足顺延**：若 V2 首次审查日完整买卖回合 < 30，判定样本不足，不判通过/作废，按日历月顺延 3 个月再评，并附注当期市场状态（沪深300区间涨跌 + ERP分位）；完整买卖回合 ≥30 时严格按终审三条件执行，不因市场环境顺延
- **有效验证切点**：以 `account_trend_v2.json` 的创建日为切点，从 200 万空仓独立计算全部回合和表现；旧 `account_trend.json` 只读展示，不计入 V2。日报与趋势周报同时显示有效收益、基准、超额、最大回撤和四项准入条件
- **终审结果**：
  - 通过 → 小规模上实盘（50 万起步，非 200 万），继续观察 6 个月后再决定加码
  - 不通过 → 策略作废，不再纠结。不调参重试——一次不通过说明回测超额不可靠，任何参数调整都是二次过拟合

## 筛选流程
**深价池**：量价初筛（跌幅≥40%+缩量+低价分位）→ 商品周期检测 → 行业过滤 → 财务验证 → `candidates.csv`
**趋势池**：经校验的沪深300母集 → 同报告期利润YoY改善 → 质量过滤 → TOP10 → `trend_candidates.csv`。趋势 V2 原策略会自动消费该文件；增强研究结论不改变 V2 订单。

## 候选池追踪（假设性买入）
- 每只候选从首次入选起记录虚拟买入（入场日期+入场价），每日更新盈亏、最大盈利、最大回撤
- 退出候选池时自动结算，记录最终收益
- 数据存 `output/candidate_tracker.csv`，日报和周报均有追踪摘要
- 目的：闭环验证筛选器有效性，不让候选池"推荐完不管"

## 深度分析规范（买入决策前必填）
每只考虑买入的股票必须在 `output/research/` 下创建分析记录，格式参照 `output/watchlist_growth.md`。不写分析 = 不买入。
- **文件名**：`output/research/{股票代码}.md`
- **必含维度**：业务驱动力 / 增长质量（利润+营收趋势，非一次性收益占比） / 竞争格局 / 管理层行为 / 问题可逆性 / 买入理由一句话 / 不买理由一句话
- **决策日志**：每次研究后记录日期 + 结论（买入/观望/淘汰）+ 关键理由。事后对照实际走势，验证判断质量
- 目录结构约定：`output/research/` 入 git，与候选池、持仓联动审查
- **可执行首仓建议**：只有明确建议普通深价虚拟仓首次买入时，才按 `output/research/README.md` 在研究文件顶部填写 YAML；缺失或校验失败均不生成订单

### AI 异步深度分析队列
日更检查 `candidates.csv` 和 `trend_candidates.csv`，列出缺少 `output/research/{代码}.md` 的股票。该列表只是研究队列信号，GitHub Actions 不调用大模型，也不会自动完成研究；必须由独立的 Codex/人工研究任务接管：
1. 读取该股票的日K线数据（`data/daily_kline/`）、最新财务数据（`data/financials/`）、行业分类（`data/market/stock_industry_map_v2.json`）
2. 按 7 维度模板（业务驱动力/增长质量/竞争格局/管理层行为/问题可逆性/买入理由/不买理由）生成分析
3. 写入 `output/research/{股票代码}.md`
4. 汇报结论：建议买入/观望/淘汰，附关键理由

普通深价最小自动首仓闭环已实施；完整五问、90 天研究生命周期和前瞻分组追踪暂缓。实施边界见 `docs/superpowers/specs/2026-08-07-minimal-deep-entry-design.md`。不得把最小结构化建议宣称为已通过五问。

## 开发约定
- 日更后检查待分析候选并输出研究队列；GitHub Actions 不调用大模型，研究文件只能由独立 AI/人工任务写入
- 数据源：akshare（免费）
- 数据缓存：日K线TTL=1天，财务数据TTL=30天，趋势扫描原始DataFrame缓存30天
- 财务数据混合口径：ROE/CF/EPS取最新年报（12-31），利润/营收YoY取最新报告期（含季度），资产负债率取最新报告期
- 选股K线拉取并行化（ThreadPoolExecutor 10线程），K线内存缓存防重复拉取
- 日更：`python main.py daily`（本地）或 Actions 自动（工作日17:30），本地~60秒，Actions ~12分钟
- 日报顶部提供次日开盘前使用的“晨间执行卡”：先显示数据截止日、双仓账户浮盈亏金额、待执行订单和待分析股票；持仓表逐票明确显示仓别（深价仓/趋势V2）、浮盈亏金额、盈亏率与本轮持仓自然日（加仓不重置，无法从交易流水可靠重建时显示“未知”）。晨间卡只摘要，完整研究和审计信息继续保留在正文。
- 日报明确隔离 `增强研究观察（不影响趋势 V2）` 与 `V2 明日执行清单（原策略）`；研究建议不得表述为 V2 订单，V2 订单也不得表述为已通过增强研究。终端保持表格化，存档转为 GitHub Markdown，并刷新 `output/reports/README.md`
- 容错：`data_fetcher.py` 全局 monkey-patch requests 设 15s 超时，所有网络调用加 try/except，失败降级到缓存
- 基准价获取：缓存优先（当日缓存直接读）；缺当日数据时以新浪 `stock_zh_index_daily` 为主、东财 `stock_zh_index_daily_em` 兜底；全部失败打印告警并沿用缓存，避免静默失真
- 绝对交易日熔断：用新浪交易日历（`data/market/trade_calendar.csv`）和 Asia/Shanghai 16:30 截止线推导期望交易日；日历不可判定时双仓 fail-closed。深价仓、趋势仓分别核对全部持仓 K 线，任一缺失/过期只冻结对应仓的买卖及账户/持仓/表现写入，候选研究继续；趋势候选 K 线过期直接淘汰；沪深300基准过期只冻结基准/超额表现写入，不阻断数据新鲜仓的交易
- 参数化：策略阈值统一放 config.yaml
- 每周五复盘时回填 `output/market_judgment_log.md`：逐条判定历史市场判断命中/未命中/待验证，命中率是系统可靠性的唯一客观指标
- 交易成本模型：自动日更账户启用佣金（万2.5，最低5元）/印花税（卖出0.05%）/滑点（0.1%）/一字板近似；参数在 `config.yaml` `costs`；历史交易不追溯，回测基线保持无成本
- T+1 幂等：同一仓、同一方向、同一信号日一旦形成订单批次，当日重跑不得追加新标的；终结订单继续留在账本中审计
- 周报/月报含双策略组合总览：组合口径=深价100万+趋势200万按初始资金权重合成，输出累计收益/最大回撤/年化波动率/日收益相关度；沪深300累计按基准缓存 close 计算（`performance*.csv` 中 7/24 起基准列沿用旧 4645.95 属历史口径）
- git：`data/daily_kline/` 和 `data/financials/` 不入库；`data/market/` 和 `output/`（含 `research/`）入库存档；每次日更后提交
- 改策略参数前先跑回测验证，回测支持 `config_overrides` 参数覆盖和 `mode` 切换
- **停止调参**：策略参数固定为当前基线，不再为回测成绩调整。唯一验证是趋势仓纸上跑未知数据（2026-07-08起）
- 买入决策：必须按「深度分析规范」章节执行，书面分析记录存入 `output/research/`，不写不买
- 趋势虚拟仓自动执行但仅供观察，不与主仓资金混合
- ⚠️ PowerShell 5.1 读 UTF-8 无 BOM 脚本会吞中文
- **日K复权口径**：个股日K统一使用前复权（`qfq`），主备数据源必须保持相同口径；缓存键包含 `qfq` 版本，禁止读取旧版未复权缓存计算52周跌幅、价格分位、MA200或移动止盈。该规则只修正价格数据，不改变策略阈值
- Windows 控制台保留当前编码并以替代字符显示无法编码的 emoji；Markdown 报告保持完整 UTF-8。需要完整终端符号时设置 `$env:PYTHONIOENCODING='utf-8'`
